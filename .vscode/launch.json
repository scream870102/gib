{
    "version": "0.2.0",
    "configurations": [
        {
        "name": "Debug Gib",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}",
        "envFile": "${workspaceFolder}/.env"
        }
    ]
}